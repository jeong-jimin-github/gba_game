name: Build and Release GBA Game

on:
  push:
    branches:
      - main
  release:
    types:
      - created

jobs:
  build-and-release:
    name: Build and Release GBA File
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up environment
      env:
        DEVKITPRO: /opt/devkitpro
        DEVKITARM: /opt/devkitpro/devkitARM
      run: |
        sudo apt-get update
        sudo apt-get install -y git build-essential
        wget https://github.com/devkitPro/installer/releases/latest/download/devkitpro-pacman.deb
        sudo dpkg -i devkitpro-pacman.deb
        sudo dkp-pacman -Syu --noconfirm
        sudo dkp-pacman -S gba-dev --noconfirm

    - name: Build project
      env:
        DEVKITPRO: /opt/devkitpro
        DEVKITARM: /opt/devkitpro/devkitARM
      run: |
        make clean
        make

    - name: Upload GBA file as artifact
      if: github.event_name == 'push'
      uses: actions/upload-artifact@v4
      with:
        name: gba-game
        path: roms/*.gba

    - name: Upload GBA file to release
      if: github.event_name == 'release'
      uses: actions/github-release@v2
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        files: roms/*.gba
        draft: false